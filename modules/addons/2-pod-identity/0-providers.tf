############################################################
# 🌐 Provider Requirements — EKS Pod Identity Addon
############################################################

terraform {
  required_providers {
    aws = { source = "hashicorp/aws" }
  }
}
